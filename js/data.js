'use strict';

(function () {
  var WIDTH_PIN = 50;
  var HEIGHT_PIN = 70;

  var map = document.querySelector('.map');

  window.data = {
    WIDTH_PIN: WIDTH_PIN,
    HEIGHT_PIN: HEIGHT_PIN,
    map: map
  };
})();
